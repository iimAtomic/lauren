PORT=3000
MONGODB_URI=mongodb+srv://lux:luxluxlux@cluster0.8sx89.mongodb.net/lauren?retryWrites=true&w=majority

# Optionnel : secret dédié aux cookies session (≥ 24 caractères). Sinon dérivé de ADMIN_KEY en prod.
# SESSION_SECRET=
ADMIN_KEY=bruh
# Numéro WhatsApp pour les commandes : indicatif pays + numéro, chiffres uniquement (défaut serveur : 22991180721)
WHATSAPP_ORDER_NUMBER=22991180721
SESSION_SECRET=luxluxlux

# Production Vercel :
# - Copier ces variables dans Project → Settings → Environment Variables (Production).
# - Atlas : Network Access → 0.0.0.0/0 (ou IP sortantes Vercel).
# - Ne pas mettre CORS_ORIGINS sauf si tu veux restreindre : sinon toutes origines + cookies autorisés.
# - Vérifier /api/health : {"ok":true,"mongo":1} quand la base répond (1 = connecté).
