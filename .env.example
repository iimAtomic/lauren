PORT=3000
MONGODB_URI=mongodb+srv://lux:luxluxlux@cluster0.8sx89.mongodb.net/lauren?retryWrites=true&w=majority

# Numéro WhatsApp pour les commandes : indicatif pays + numéro, chiffres uniquement (défaut serveur : 22991180721)
WHATSAPP_ORDER_NUMBER=22991180721

# Vercel : mêmes variables en Production.
# Atlas : Network Access 0.0.0.0/0 + cluster dans la MEME REGION que la fonction Vercel
# (sinon 504 / cold start trop long). Région Vercel fixée dans vercel.json (par défaut iad1 = AWS us-east-1).
# Plan Hobby : ~10s par requête API max — la connexion Mongo doit rester courte.
# Images (admin) : MongoDB GridFS, URL publique = /api/media/<id> (aucune variable Blob/S3).
